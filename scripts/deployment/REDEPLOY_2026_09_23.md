# Full redeployment and automatic participant Excel emails — 23 September 2026

The complete backend and production frontend build were redeployed to 172.105.47.106. Migrations 0162–0165 applied successfully, including participant logo/documents and the email delivery log. Production database content, uploaded media and existing integration settings were preserved.

- Backup: `/var/backups/goimomi/full-release-20260923T061624/`
- Previous frontend: `/var/www/goimomi/goimomifrontend/dist-previous-20260923T061624`
- Release SHA-256: `f9d0adab7930f3933fb5be37c16d922518730d2f004ab1639bb2794d0687c163`
- Deployment log: `/root/goimomi-import/auto-exports-release.log`
- Configured recipient: `chithiraibiz@gmail.com`. No other recipients were supplied. The comma-separated `GLOBAL_HORIZONS_EXPORT_RECIPIENTS` setting supports multiple recipients.

Every committed Global Horizons profile creation, update or deletion queues a full current Excel report. Internal document-only saves do not duplicate notifications. The existing `enquiries` worker sends mail; `goimomi-profile-exports.timer` republishes due pending records every minute. Five failed SMTP attempts leave a Failed record visible in Django admin, with a manual retry action. Recipient lists are captured per delivery and sent using BCC. The attachment includes all profiles and all model fields, with absolute photo/logo/document URLs.

The initial full report included 2 profiles. Delivery record 1 was accepted by the SMTP backend on its first attempt at 2026-09-23 06:18:00 UTC. This verifies worker execution, attachment generation and SMTP acceptance; inbox receipt cannot be confirmed from the server.

Validation: 59 isolated backend tests, 9 frontend tests, production build, migration consistency check, Nginx configuration, active application/worker/timer services, and live browser checks for participant upload/submission/retry/mobile layout plus admin search/view/edit/add/cancel/delete. Browser writes were mocked; automated tests used isolated databases and in-memory mail. Production participant data was not edited for verification.

## CC update — 06:26 UTC

Deployed the automatic Excel email CC update. To: chithiraibiz@gmail.com. CC: support@goimomi.com. Additional main recipients, if configured later, remain BCC. Notification tests passed locally and on the server (8 tests); application, worker and retry timer are active and the live participant page returns HTTP 200.

A verification full Excel report (delivery 2) was accepted by SMTP on the first attempt at 2026-09-23 06:26:29 UTC. Backup: `/var/backups/goimomi/export-cc-20260923T062551/`. Production environment ownership and permissions are ubuntu:ubuntu, 0600.

## Attending poster redesign — 06:35 UTC

Updated the generated social poster to match the supplied Sri Lanka campaign reference: scenic AI-generated backdrop, original event emblem, green/gold WE ARE ATTENDING headline, participant portrait/details panel, original brand marks and connection message. Output remains 1080 x 1350 PNG with matching PDF.

Background: `goimomibackend/Holidays/static/global_horizons/attending-landscape-v2.png`; prompt saved alongside it. Created using the built-in image generation tool. Sample and long-name visual checks are in `output/global-horizons/poster-v2/`.

All 17 participant/document tests passed on the server. Refreshed both existing participant posters without changing participant information or triggering Excel notifications. Previous media files were retained so earlier links continue working. Backup: `/var/backups/goimomi/poster-v2-20260923T063550/`.
