# Full redeployment — 22 September 2026

Deployed the complete production frontend build and backend source to 172.105.47.106, including the redesigned Golden Triangle page and generated Taj Mahal and Jaipur images.

- Live page: https://goimomi.com/golden-triangle
- Source revision: 10fc081
- Release archive SHA-256: 4e86d75d4c420774cea7ffce002ee9f4d9a177d806ae33201f7d9086aa3516ff
- Database and application backup: `/var/backups/goimomi/full-release-20260922T123448/`
- Previous frontend: `/var/www/goimomi/goimomifrontend/dist-previous-20260922T123448`
- Server deployment log: `/root/goimomi-import/full-release.log`

Production environment settings, database content, and uploaded media were preserved. Dependencies matched; pip check passed. Django checks and migration checks passed; no database migrations were pending. Static files were collected. Nginx configuration passed. The goimomi, goimomi-enquiries, and nginx services are active.

Verification: production build, 9 frontend tests, 34 isolated backend tests, live desktop/mobile browser checks (API submissions mocked), six package prices, five itinerary days, generated image loading, HTTPS and HTTP redirect, public API responses, admin login and refresh. A production enquiry returned HTTP 201 in 0.157 seconds inside a rolled-back transaction with email/CRM dispatch suppressed.

The Zoho webhook readiness endpoint returned HTTP 200. This deployment does not establish successful external Zoho CRM delivery, email delivery, or payment processing; those were not exercised.

## Second full release — 15:45 UTC

Deployed the current working tree, including Global Horizons participant forms, admin management, photo uploads, Excel/PDF exports and migrations 0159–0161. Added openpyxl 3.1.5. Database migrations applied successfully after backup; production settings and existing media were preserved.

- Backup: `/var/backups/goimomi/full-release-20260922T154542/`
- Previous frontend: `/var/www/goimomi/goimomifrontend/dist-previous-20260922T154542`
- Main release SHA-256: `058c863cff987d55e13a20f72b9ed7ab0ba72505dbd1b027f651984806178b4a`
- The release includes a supplemental `test_global_horizons.py` patch clearing cached throttle counters between isolated tests. Initial combined tests stopped deployment safely; all 43 backend tests passed after this test-only fix. Production throttling was not changed.
- Build and 9 frontend tests passed. Live Global Horizons browser checks passed with API writes mocked. Public pages and APIs returned 200; unauthenticated participant lists and exports returned 401. Admin login, token refresh and a rolled-back enquiry save passed (0.144 seconds, notifications suppressed).
- Live participant form: https://goimomi.com/globalhorizonssrilanka
- Final live Golden Triangle/existing-site browser checks passed, including pricing, itinerary, enquiry payload and desktop/mobile layout. Production homepage loading exceeded the initial 30-second network-idle wait; the suite passed with a 90-second navigation allowance. The browser script now accepts `VERIFY_NAVIGATION_TIMEOUT_MS` for this purpose.
- Staff-only live list and real Excel/PDF exports returned 200; final migration check passed.

## Global Horizons SEO and optional links — 16:46 UTC

Published route-specific title, description, canonical, Open Graph/Twitter previews and Sri Lanka share artwork for `/globalhorizonssrilanka/` and its older `/global-horizons-srilanka/` alias. Both canonicalize to `/globalhorizonssrilanka/`. Corrected the sitemap generator to emit unique canonical URLs, including consistent trailing slashes (39 unique URLs).

The optional field now accepts website or social-profile HTTP/HTTPS links. Bare domains and social-profile paths are normalized to HTTPS by the backend. Blank values remain accepted. Unsupported schemes, whitespace within URLs, embedded credentials and overlong links are rejected. Existing values and database records were preserved; no migrations were needed.

Production rollback: `/var/backups/goimomi/full-release-20260922T164648/`. All 46 staged backend tests and 9 frontend tests passed. Local browser checks covered a bare LinkedIn URL and static/runtime metadata. Live server checks confirmed both route previews and the shared JPEG respond correctly. The sitemap was subsequently updated separately to remove duplicates.
